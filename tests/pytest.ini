[pytest]
log_cli = True
log_cli_level = INFO
markers =
    auth_required: mark test as requiring authentication.
    editor: checks the boundaries of the editor role.
    poet: checks the boundaries of the poet role.